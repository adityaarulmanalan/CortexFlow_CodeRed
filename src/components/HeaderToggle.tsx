"use client";

// HeaderToggle is no longer used — role is determined by login authentication.
// Keeping this file to avoid import errors but it renders nothing.

export function HeaderToggle() {
  return null;
}
